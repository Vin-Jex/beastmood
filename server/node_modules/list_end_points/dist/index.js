"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const main_1 = __importDefault(require("./src/main"));
require("colors");
const listEndpoints = (app) => {
    (0, main_1.default)(app).forEach((endpoint, key, arr) => {
        console.log(`${key + 1}`.blue, endpoint.methods.includes('GET')
            ? 'GET'.green.bold
            : endpoint.methods.includes('PUT')
                ? 'PUT'.blue.bold
                : endpoint.methods.includes('POST')
                    ? 'POST'.yellow.bold
                    : endpoint.methods.join(', ').red.bold, endpoint.middlewares.join(' ').cyan, endpoint.path.yellow.bold);
        if (key === arr.length - 1) {
            console.log(`${arr.length} endpoints registered`.yellow.underline);
        }
    });
};
exports.default = listEndpoints;
